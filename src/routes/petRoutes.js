const express = require('express');
const router = express.Router();
const petController = require('../controllers/petController');

// Implemente as rotas do pet conforme necessário

module.exports = router;
